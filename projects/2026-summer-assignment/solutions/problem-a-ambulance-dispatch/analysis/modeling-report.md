# A题《某城市急救中心救护车站点选址与调度优化》题目分析报告

> **状态说明（2026-08-11）**：任务一模型与结果继续有效。任务二原“无条件NHPP日总量 + 每车12次硬上限”合同及其全部数值结果已经废止，不得用于论文。现模型固定每日140次呼叫，仅按周期双高斯强度随机化到达时刻，并采用相对4 min黄金时限的延迟惩罚成本；任务二已按固定30天预热、30个独立最终复制重新计算。

> 当前阶段：任务一、任务二已获得真实计算结果；任务三以任务二入选方案为条件输入
> 原题文件：`../../problem-statements/problem-a-ambulance-dispatch-statement.docx`  
> 原题 SHA-256：`5F5079815AB8AD6592FEE7A4B0B8B01A5DF8865983A2871C324B6AB772C39F2D`  
> 原题数据：3张表，无逐条呼叫时间戳、无道路网络、无独立数据附件  
> 目标任务：课程暑期作业；论文借用用户提供的2026国赛Word模板样式，但不得声明为2026国赛参赛论文
> 本报告用途：作为后续代码、图表和论文的统一模型合同；与本报告冲突的旧草稿或队友方案不再作为实现依据

## 1. 原题事实与两问之间的逻辑

### 1.1 题面要求

任务一要求以平均出车距离和服务覆盖率为目标，建立站点选址优化模型并推荐站点。任务二要求利用给定呼叫数据设计包含就近派车和备用车辆配置的调度方案，计算优化后的平均响应时间。

两问不是两个互不相关的模型。任务一确定车辆的常驻站点和数量；任务二以该配置为固定输入，研究车辆忙碌、排队和备用规则下的逐单派车。任务一中的分配量是长期平均流量，不等于任务二中的刚性服务辖区，任务二允许跨站支援。

### 1.2 原题数据

10个区域的日均呼叫量为

$$
q=(28,15,18,10,8,6,20,22,9,4),\qquad \sum_{i=1}^{10}q_i=140\ \text{次/日}.
$$

6个候选站点的最大配车数为

$$
\bar v=(3,2,2,2,1,2),\qquad \sum_{j=1}^{6}\bar v_j=12\ \text{辆}.
$$

题面还给出：每车每日最多出诊12次，车辆平均速度45 km/h，准备时间3 min，单次任务平均占用45 min，黄金响应时限4 min，标准站点年成本120万元，延迟惩罚成本200元/(min·次)。

### 1.3 数据能力边界

原题所谓“历史急救呼叫数据”实际上只有区域日均呼叫量，没有逐条到达时刻、逐小时频数、伤情等级、真实响应记录或道路行程时间。因此：

- 可以建立由日均量约束的合成随机呼叫情景；
- 不能声称日内峰谷是从历史数据拟合得到的；
- 不能凭空添加伤情等级比例或优先队列；
- 距离按题注使用区域中心与站点坐标的欧氏距离；
- 区域“距最近医院距离”不是救护站到呼叫点的出车距离，不进入到达现场的响应时间；
- 45 min按完整车辆占用周期处理，不能再额外叠加准备、往返和现场时间。

## 2. 统一假设、定义与不可混用的口径

### 2.1 基础假设

1. 每次呼叫发生在对应区域中心；空间误差留待敏感性分析，不在主模型中虚构区内分布。
2. 站点与区域之间采用欧氏距离，车辆平均速度恒为45 km/h。
3. 响应时间指呼叫到达系统至救护车到达现场，不含之后的现场处置或送医时间。
4. 单次任务自车辆接受派车起占用45 min；45 min后车辆回到所属站点并恢复可派。
5. 车辆不在任务结束地点长期驻留，不跨站改变常驻归属，除非后续任务三另行允许。
6. 日常呼叫同质，等待队列采用全市统一先到先服务（FCFS）。
7. 每车每日最多接受12次任务；次数按接受派车的日历日归属。

### 2.2 距离与响应时间

区域 $i$ 到站点 $j$ 的距离为

$$
d_{ij}=\sqrt{(x_i-X_j)^2+(y_i-Y_j)^2}\quad (\text{km}).
$$

任务二中呼叫 $e$ 的响应时间为

$$
T_e^{\mathrm{resp}}
=T_e^{\mathrm{wait}}+3+\frac{60d_e}{45}
=T_e^{\mathrm{wait}}+3+\frac{4}{3}d_e\quad (\text{min}).
$$

无等待时，4 min内只有1 min可用于行驶，严格黄金覆盖半径为

$$
R_4=45\times\frac{4-3}{60}=0.75\ \text{km}.
$$

若已经等待 $w$ min，则剩余有效覆盖半径为

$$
R_4(w)=0.75(1-w),\qquad 0\le w\le1.
$$

当 $w>1$ 时，即使车辆位于呼叫点，响应时间也已超过4 min。

### 2.3 三种覆盖率必须分别报告

1. **严格4分钟覆盖率**：实际或静态响应时间满足 $T^{\mathrm{resp}}\le4$ 的比例；无等待时半径为0.75 km。
2. **仅行驶4分钟覆盖率**：错误地把完整4 min用于行驶，对应3 km；只能作为对照，不能称为黄金4分钟覆盖率。
3. **静态潜在3 km覆盖率**：只判断区域附近是否存在3 km内的站点，不考虑容量、车辆忙碌和实际分配；它比第二种口径还乐观。

## 3. 任务一：容量约束站点—车辆—服务分配

### 3.1 任务一最终要回答什么

任务一应给出并证明：

- 哪些候选站必须启用；
- 每个站点必须配置多少辆车；
- 140次日均呼叫如何在6站之间分配；
- 最小加权出车距离和无排队响应时间下界；
- 在不牺牲最小距离的前提下，可达到的严格4分钟静态覆盖率；
- 为什么97.1%不能被写成完整4分钟覆盖率。

题面虽给出标准站点成本，却没有总预算；也没有限制最多建设几个站点。因此不擅自添加预算约束，不做“预算—未服务量”Pareto选址。建设优先级评分不进入主模型，因为它既不是硬约束，也不是用户选定的优化目标。

### 3.2 完整混合整数线性规划

#### 3.2.1 集合与参数

- 需求区 $i\in I=\{1,\ldots,10\}$；
- 候选站 $j\in J=\{1,\ldots,6\}$；
- $q_i$：区域 $i$ 的日均呼叫量，单位为次/日；
- $d_{ij}$：区域 $i$ 到站点 $j$ 的距离，单位为km；
- $\bar v_j$：站点 $j$ 的最大配车数；
- $c=12$：每车每日最大出诊次数。

#### 3.2.2 决策变量

- $x_{ij}\ge0$：区域 $i$ 的日均呼叫中由站点 $j$ 承担的数量，单位为次/日；
- $v_j\in\mathbb Z_+$：站点 $j$ 的配车数；
- $y_j\in\{0,1\}$：站点 $j$ 是否启用。

$x_{ij}$是长期平均流量，可取连续值；它不是单次呼叫的0-1派车变量。

#### 3.2.3 第一层目标：最小化加权出车距离

$$
\min D=\sum_{i\in I}\sum_{j\in J}d_{ij}x_{ij}.
$$

因为总需求固定为140，最小化 $D$ 与最小化加权平均距离 $\bar d=D/140$ 等价。

#### 3.2.4 约束

需求守恒：

$$
\sum_{j\in J}x_{ij}=q_i,\qquad \forall i\in I.
$$

站点服务能力：

$$
\sum_{i\in I}x_{ij}\le 12v_j,\qquad \forall j\in J.
$$

车辆上限与站点启用联动：

$$
y_j\le v_j\le \bar v_jy_j,\qquad \forall j\in J.
$$

全市车辆总数限制：

$$
\sum_{j\in J}v_j\le12.
$$

变量域：

$$
x_{ij}\ge0,\qquad v_j\in\mathbb Z_+,\qquad y_j\in\{0,1\}.
$$

### 3.3 六站满配的必要性证明

由需求守恒，全部站点承担的总量为

$$
\sum_j\sum_i x_{ij}=\sum_i q_i=140.
$$

再对站点容量约束求和：

$$
140=\sum_j\sum_i x_{ij}\le12\sum_jv_j.
$$

因此

$$
\sum_jv_j\ge\frac{140}{12}=11.667.
$$

由于 $v_j$ 为整数且全市最多12辆，必有

$$
\sum_jv_j=12.
$$

同时

$$
\sum_j\bar v_j=3+2+2+2+1+2=12,
$$

而逐站满足 $v_j\le\bar v_j$。只有每一项都达到上限，才可能使总和等于12。因此唯一可行配车为

$$
(v_1,v_2,v_3,v_4,v_5,v_6)=(3,2,2,2,1,2),
$$

并由联动约束推出

$$
(y_1,y_2,y_3,y_4,y_5,y_6)=(1,1,1,1,1,1).
$$

这不是根据优先级评分作出的经验推荐，而是“140次需求全部覆盖、每车12次、总车数至多12、各站停车容量”共同强制得到的必要结论。任何关闭一个站点或让任一站点少配一辆车的方案，最多只有11辆车和132次/日服务能力，必然违反需求守恒。

### 3.4 约化为运输型线性规划

配车和开站变量被可行性唯一确定后，模型约化为

$$
\min_{x_{ij}}\sum_{i,j}d_{ij}x_{ij}
$$

满足

$$
\sum_jx_{ij}=q_i,\qquad
\sum_ix_{ij}\le(36,24,24,24,12,24)_j,\qquad
x_{ij}\ge0.
$$

这就是容量约束运输型线性规划。它既允许某个区域拆分给多个站点，也避免把“最近站分配”误当成可执行方案。

### 3.5 第二层目标：在距离最优解中最大化严格覆盖

定义

$$
g_{ij}=\mathbf1\left(3+\frac{60d_{ij}}{45}\le4\right)
=\mathbf1(d_{ij}\le0.75).
$$

先求得最小总距离 $D^*$，再解

$$
\max C_4=\sum_{i,j}g_{ij}x_{ij}
$$

并增加

$$
\sum_{i,j}d_{ij}x_{ij}\le D^*+\varepsilon,
$$

其中 $\varepsilon$ 只用于数值容差。该词典序结构保证覆盖率优化不能牺牲第一层已经取得的最小距离。

### 3.6 独立复算结果

当前建模阶段使用SciPy/HiGHS对上述两阶段线性规划进行了独立复算，求解退出码为0。得到完整服务分配矩阵如下，单位为次/日：

| 区域 | S1 | S2 | S3 | S4 | S5 | S6 | 行和 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | 28 | 0 | 0 | 0 | 0 | 0 | 28 |
| 2 | 0 | 15 | 0 | 0 | 0 | 0 | 15 |
| 3 | 0 | 0 | 18 | 0 | 0 | 0 | 18 |
| 4 | 0 | 0 | 0 | 10 | 0 | 0 | 10 |
| 5 | 0 | 0 | 0 | 0 | 8 | 0 | 8 |
| 6 | 0 | 0 | 0 | 0 | 0 | 6 | 6 |
| 7 | 0 | 4 | 6 | 6 | 4 | 0 | 20 |
| 8 | 0 | 0 | 0 | 4 | 0 | 18 | 22 |
| 9 | 8 | 1 | 0 | 0 | 0 | 0 | 9 |
| 10 | 0 | 0 | 0 | 4 | 0 | 0 | 4 |
| 站点负荷 | 36 | 20 | 24 | 24 | 12 | 24 | 140 |

约束核对：每个区域行和等于其需求量；站点负荷不超过 $(36,24,24,24,12,24)$；只有S2保留4次/日容量余量，与全市总余量4一致。

数值结果为

$$
D^*=131.819720\ \text{km·次/日},
$$

$$
\bar d=\frac{D^*}{140}=0.941569\ \text{km},
$$

$$
\bar T_{\mathrm{static}}
=3+\frac{60\bar d}{45}
=4.255426\ \text{min}.
$$

$\bar T_{\mathrm{static}}$没有考虑车辆忙碌和排队，只能作为任务二实际平均响应时间的静态下界，不能直接写成任务二答案。

### 3.7 覆盖率口径核对

在严格0.75 km半径下，只有与S1--S6坐标重合的区域1--6可在无等待时满足4 min。这些区域合计需求为

$$
28+15+18+10+8+6=85,
$$

故距离最优解中的严格静态4分钟覆盖率为

$$
\rho_4^{\mathrm{static}}=\frac{85}{140}=60.714\%.
$$

另外两个仅作辨析的指标为：

- 按实际分配矩阵、但错误地给车辆完整4 min行驶时间，即3 km半径，覆盖率为 $121/140=86.429\%$；
- 只判断每个区域附近是否存在3 km内站点，不考虑容量和实际分配，静态潜在覆盖率为 $136/140=97.143\%$。

因此97.1%只能称为“3 km静态潜在覆盖率”，不能称为完整4分钟覆盖率，更不能代表车辆忙碌条件下的实际服务率。

### 3.8 任务一实现步骤

1. 从原题表格读取10个区域坐标、需求量，以及6个站点坐标和容量。
2. 计算 $10\times6$ 欧氏距离矩阵，并检查单位为km。
3. 建立完整MILP，先由模型和代数证明验证唯一配车 $(3,2,2,2,1,2)$。
4. 固定 $v,y$，求运输LP的 $D^*$。
5. 在 $D\le D^*+\varepsilon$ 条件下最大化严格覆盖量。
6. 导出完整 $10\times6$ 分配矩阵，不只报告“主服务站点”。
7. 自动检查需求守恒、站点容量、总车辆数、变量非负和两阶段目标。
8. 分别输出严格4分钟、实际分配3 km、静态潜在3 km三种覆盖率，禁止混名。

### 3.9 任务一失败条件

出现以下任一情况即判定实现错误：

- 少于12辆车却声称完全覆盖140次日均需求；
- 关闭任一站点却未引入外援或未服务变量；
- 把最近医院距离当成站点到需求点距离；
- 把建设优先级评分直接加入距离目标却不给单位转换依据；
- 只用最近站分配而不检查站点容量；
- 把0.75 km写成3 km，或把97.143%称为完整4分钟覆盖率；
- 把4.255426 min静态下界称为含排队的优化后平均响应时间。

## 4. 任务二：周期NHPP驱动的连续多日离散事件仿真

### 4.1 任务二最终要回答什么

任务二应在任务一固定站点和配车下回答：

- 日内随机呼叫如何生成，并明确哪些部分来自题面、哪些属于合成情景；
- 最近车辆忙碌或达到每日12次上限时如何排队和跨站派车；
- 基准就近派车、覆盖保护派车、固定备用配置分别如何执行；
- 跨越24:00的呼叫、车辆和每日计数如何处理；
- 三种策略的平均响应时间、严格4分钟服务率、P90/P95、等待和公平性差异；
- 固定备用配置是否在不增加平均响应时间的条件下真正有效。

### 4.2 到达过程：给定日总量的双高斯条件NHPP

#### 4.2.1 数学模型

先用非齐次Poisson过程（NHPP）的强度形状描述日内峰谷：

$$
\lambda_i(t)=q_i f(t\bmod24).
$$

$f(t)$ 的单位为 $\text{h}^{-1}$，并满足

$$
f(t+24)=f(t),\qquad \int_0^{24}f(t)\,dt=1.
$$

若不加条件，全市参考过程的一日积分为140。为严格采用题面给出的140次日均呼叫作为每日仿真规模，本模型对每天的总到达数施加条件

$$
N_d=140.
$$

根据NHPP在给定区间事件总数后的条件分布，当天140个到达时刻相互独立地服从归一化密度 $f(t)$，再按 $q_i/140$ 独立标记区域。因此每天全市呼叫数严格为140，区域计数服从多项分布，且区域 $i$ 的日呼叫期望仍为 $q_i$。条件化后不再把每日计数写成Poisson随机变量，也不声称各区域日计数相互独立。

#### 4.2.2 周期双高斯强度

为避免午夜出现人为跳变，定义周期高斯核

$$
G(t;\mu,\sigma)=\sum_{k\in\mathbb Z}
\exp\left[-\frac{(t-\mu+24k)^2}{2\sigma^2}\right].
$$

未归一化强度为

$$
g(t)=b+a_1G(t;\mu_1,\sigma_1)+a_2G(t;\mu_2,\sigma_2),
$$

归一化后

$$
f(t)=\frac{g(t)}{\int_0^{24}g(s)\,ds}.
$$

基准合成情景采用

$$
b=1,\quad
(a_1,\mu_1,\sigma_1)=(0.8,9,2),\quad
(a_2,\mu_2,\sigma_2)=(1.0,18,2.5).
$$

这组参数只表示“上午与傍晚双峰”的透明情景，不是历史拟合值。每小时概率

$$
p_h=\int_h^{h+1}f(t)\,dt,\qquad h=0,\ldots,23
$$

为：

| 时段 | $p_h$ | 时段 | $p_h$ | 时段 | $p_h$ |
|---|---:|---|---:|---|---:|
| 00--01 | 0.03020879 | 08--09 | 0.05159932 | 16--17 | 0.05346191 |
| 01--02 | 0.02953895 | 09--10 | 0.05167256 | 17--18 | 0.05759152 |
| 02--03 | 0.02940119 | 10--11 | 0.04705243 | 18--19 | 0.05758858 |
| 03--04 | 0.02976585 | 11--12 | 0.04095217 | 19--20 | 0.05343842 |
| 04--05 | 0.03111407 | 12--13 | 0.03698935 | 20--21 | 0.04686835 |
| 05--06 | 0.03432981 | 13--14 | 0.03696850 | 21--22 | 0.04019260 |
| 06--07 | 0.03992160 | 14--15 | 0.04076121 | 22--23 | 0.03503321 |
| 07--08 | 0.04671574 | 15--16 | 0.04699907 | 23--24 | 0.03183481 |

24项之和为1。后续对两个峰值幅度分别进行 $\pm10\%$、$\pm20\%$ 扰动并重新归一化，检验调度结论对日内集中程度是否敏感。

#### 4.2.3 随机生成实现

程序按给定 $N_d=140$ 的NHPP条件分布生成每天的到达时刻。以

$$
\lambda_\Sigma(t)=140f(t).
$$

作为参考强度，采用接受—拒绝采样：在 $[0,24)$ 上均匀生成候选时刻，以与 $f(t)$ 成比例的概率接受，直至取得140个时刻，再在日内排序。每个到达事件按

$$
P(I=i)=\frac{q_i}{140}
$$

标记为区域 $i$。每天独立重复这一过程并平移24 h，即得到连续多日呼叫流。该方法既保留双高斯日内相对强度，又保证每天恰有140次呼叫；代码必须固定并记录随机种子。

### 4.3 车辆、队列和事件状态

#### 4.3.1 固定车辆配置

12辆车固定归属各站：

$$
u=(3,2,2,2,1,2).
$$

每辆车 $a$ 记录：所属站点、当前忙闲状态、下一次可用时刻、当前日已接受任务数、是否为固定备用车。任务结束后车辆回到所属站点。

#### 4.3.2 45分钟占用周期

车辆在时刻 $s_e$ 接受派车后，响应时间立即由等待、3 min准备和行驶距离计算；车辆从 $s_e$ 到 $s_e+45$ min均不可派。45 min已经包含准备、行驶、任务处置和返回的完整平均占用，不再额外添加去程、返程或现场时间。

#### 4.3.3 全市FCFS队列

所有未派呼叫进入一个按到达时刻排序的队列。只有队首呼叫拥有派车权；后到呼叫不能因为距离更近而越过队首。若队首当前没有合法可派车辆，系统等待下一次车辆完成或午夜计数重置事件。

#### 4.3.4 同一时刻的事件顺序

为保证程序可复现，同一时间戳按以下顺序批处理：

1. 若为00:00，先切换日历日并重置各车“新日已接受任务数”；
2. 处理在该时刻完成任务的车辆，使其回站并恢复空闲；
3. 加入该时刻到达的呼叫；
4. 反复调用派车规则，直到队首没有合法可派车辆。

连续分布下事件精确同刻的概率几乎为0，但固定顺序可消除边界实现歧义。

### 4.4 24:00边界与跨日任务

24:00不能终止任务、清空队列或把车辆强行恢复为空闲。连续多日仿真遵循：

- 车辆忙闲状态、剩余占用时间和FCFS队列跨日继承；
- 00:00只开启新的车辆日接单计数；
- 呼叫的响应指标按呼叫到达日归属；
- 车辆的出诊次数按接受派车日归属；
- 23:40接受的任务继续到次日00:25才恢复可派；
- 23:55到达、次日00:05派车的呼叫计入前一日呼叫指标，但该次任务计入次日车辆出诊次数；
- 最后一个统计日24:00停止生成新呼叫，随后继续仿真并允许跨越零点，直到所有已生成呼叫完成。

该边界仍然必要。虽然每天固定140次，低于12辆车合计144次的日接单上限，但余量仅4次；双峰到达、45 min占用和单车12次限制仍可能使临近午夜的呼叫排队，或使23时后接受的任务跨到次日。因而跨日继承用于忠实处理时序拥堵，而不是由随机日总量超过144所驱动。

### 4.5 策略A：全局就近派车基准

对队首呼叫 $e$，候选集为当前空闲且在接受该任务后仍不超过当日12次上限的所有车辆。策略A选择所属站点到呼叫区域距离最小的车辆：

$$
a_A^*=\arg\min_{a\in\mathcal A_e(t)}d_{i(e),j(a)}.
$$

同距离时依次选择当日接单较少、站点编号较小、车辆编号较小的车辆。策略A允许跨站支援，但车辆任务结束后仍回原站。它是不含覆盖保护和备用身份的基准方案。

### 4.6 策略B：45分钟期望累计响应损失与日负荷联合派车

策略B不设置备用车辆，所有12辆车都可普通派出。其目的不是机械保护某辆车，而是在当前患者的额外行驶代价、派车后未来45 min的期望累计响应损失和每日任务额度消耗之间进行权衡。45 min与题面给出的完整车辆占用周期一致，因此不是另设的预测窗口。

#### 4.6.1 当前呼叫额外响应代价

设候选车辆 $a$ 对当前区域的无等待响应时间为

$$
T_{ea}^{0}=3+\frac{60d_{i(e),j(a)}}{45}.
$$

相对最近候选车的额外代价为

$$
\Delta T_{ea}=T_{ea}^{0}-\min_{b\in\mathcal A_e(t)}T_{eb}^{0}.
$$

只保留满足

$$
\Delta T_{ea}\le\delta
$$

的候选车，防止为了未来覆盖而严重牺牲当前患者。

#### 4.6.2 45分钟期望累计响应损失

原先只判断某区域是否失去0.75 km严格覆盖，会对本来就不存在严格覆盖车辆的区域7--10产生零损失，也会在高负荷、当前无空闲覆盖车辆时退化。因此改用连续的下一呼叫响应损失，并按未来45 min的期望呼叫数累计。

在时刻 $t$ 的车辆状态 $S(t)$ 下，不考虑尚未实现的其他未来呼叫，只预测各车已有任务和日接单上限造成的最早合法可派时刻。若区域 $r$ 在未来时刻 $u$ 出现一条假想呼叫，记其预计最短响应时间为

$$
\widehat T_r(u\mid S(t))
=\min_{b\in\mathcal A}
\left[
W_b(u\mid S(t))+3+\frac{60d_{r,j(b)}}{45}
\right],
$$

其中 $W_b(u\mid S(t))$ 是车辆 $b$ 从 $u$ 起还需等待多久才具有合法接单资格：同时考虑已有任务的剩余占用时间和当日12次上限；若已达上限，则至少等待到下一个00:00，若届时仍忙则等待到实际恢复时刻。

设 $S^{(-a)}(t)$ 为当前把候选车辆 $a$ 派给队首呼叫后的反事实状态。在该状态下，车辆 $a$ 从 $t$ 到 $t+0.75$ h不可用，其他车辆状态不变。定义

$$
C_a(t)=
\int_t^{t+0.75}
\sum_{r=1}^{10}\lambda_r(u)
\left[
\widehat T_r(u\mid S^{(-a)}(t))
-\widehat T_r(u\mid S(t))
\right]\,du.
$$

$\lambda_r$ 的单位为次/h，方括号为min，积分变量为h，因此 $C_a(t)$ 是未来45 min内预期新增响应延误的总分钟数。它对全部10个区域均有效，并同时反映车辆位置、现有忙碌状态、日接单额度和时变呼叫强度。该指标是一阶滚动前瞻：只评价“当前这次派车”造成的边际影响，不把未知的未来随机呼叫当作已知信息。

#### 4.6.3 当日负荷惩罚

设车辆 $a$ 在当前日已经接受 $n_{a,d}$ 次任务，定义

$$
B_a(t)=\frac{n_{a,d}+1}{12}.
$$

它用于避免少数位置优越的车辆过早耗尽12次额度。

#### 4.6.4 派车评分

$$
J_{ea}=\Delta T_{ea}+C_a(t)+\beta B_a(t),
$$

其中三项单位均为min，$\beta$ 的单位为min。$C_a(t)$ 已经由期望呼叫数将未来损失换算为总响应分钟，因此不再另设覆盖权重。策略B选择约束内 $J_{ea}$ 最小的车辆；并列时依次比较 $\Delta T_{ea}$、$n_{a,d}$ 和车辆编号。

#### 4.6.5 参数标定

粗网格为

$$
\beta\in\{0,0.5,1,2,4\}\ \text{min},
$$

$$
\delta\in\{0.5,1,1.5,2\}\ \text{min}.
$$

共20组，先粗搜，再在优胜组合附近细化。筛选阶段采用3个共同随机复制，每个复制在30天预热后统计7天；入选参数再使用30个全新复制、每个统计30天进行最终评价。筛选种子与最终评价种子不重叠。

策略B内部参数选择层次为：

1. 所有呼叫最终完成、每车每日不超过12次、系统无持续发散积压；
2. 最小化平均响应时间；
3. 若与最优均值的成对差异在95%区间内无法区分，则最大化严格4分钟服务率；
4. 再比较P95响应时间和区域公平性。

实际筛选后取

$$
\beta^*=4\ \text{min},\qquad \delta^*=2\ \text{min}.
$$

最终30个独立复制中，策略B的平均响应时间为5.3432 min，相比策略A的5.6142 min减少0.2711 min，即下降4.83%；成对差值95%置信区间为 $[-0.3116,-0.2305]$ min。

### 4.7 策略C：固定备用配置

策略C与策略B是独立方案，不叠加覆盖损失评分。它明确设置固定备用身份，以回答题面“备用车辆配置”的要求。

#### 4.7.1 备用向量

设站点 $j$ 的备用车辆数为 $r_j$。每站至少保留一辆常规车：

$$
0\le r_j\le u_j-1,
\qquad
u=(3,2,2,2,1,2).
$$

因此S5不能设置备用车。全部向量共有

$$
3\times2\times2\times2\times1\times2=48
$$

种，其中全零向量是无备用基准，策略C需要比较47种非零配置。

#### 4.7.2 备用启用阈值

对队首呼叫，预测最快常规车的响应时间：

$$
\widehat T_e^{\mathrm{reg}}
=\min_{a\in\mathcal A^{\mathrm{reg}}}
\left[e_a(t)-t_e^{\mathrm{arr}}+3+\frac{60d_{i(e),j(a)}}{45}\right],
$$

其中 $t_e^{\mathrm{arr}}$ 是呼叫到达时刻，$e_a(t)$ 是考虑车辆剩余忙碌时间和每日12次限制后的最早合法接受时刻。因此该式包含呼叫从到达到当前时刻已经积累的等待。若车辆当日已经接受12次，则它最早在下一个00:00之后才重新取得接单资格；若届时仍在忙碌，则取实际任务完成时刻。

阈值枚举

$$
\tau\in\{4,5,6,7,8\}\ \text{min}.
$$

仅当

$$
\widehat T_e^{\mathrm{reg}}>\tau
$$

且当前存在合法空闲备用车，其从呼叫到达起算的立即派车响应时间

$$
T_{ea}^{\mathrm{res}}=t-t_e^{\mathrm{arr}}+3+\frac{60d_{i(e),j(a)}}{45}
$$

严格小于 $\widehat T_e^{\mathrm{reg}}$ 时，才启用其中响应最快的备用车。否则由当前空闲常规车中的最近者派出；若没有合法空闲常规车，队首继续等待，并在下一事件后重新计算阈值。备用车完成45 min任务并回到所属站点后，自动恢复备用身份。

47个备用向量与5个阈值组合，共235种非零备用方案。

#### 4.7.3 备用配置选择与相对代价

令第 $m$ 个共同随机复制中，策略C与A的平均响应时间差为

$$
D_m=\bar T_{C,m}-\bar T_{A,m}.
$$

筛选阶段先最小化 $\bar D$，即在全部235种方案中寻找相对A增加响应时间最少的备用向量与阈值；若均值相同，再依次比较严格4分钟服务率、P95和公平性。这样即使所有固定备用方案都略慢于A，仍会给出题面要求的最佳备用配置，而不是把策略C整体删除。

最终评价使用全新30个复制，并计算 $E[D]$ 的单侧95%置信上限 $U_{0.95}$：

- $U_{0.95}\le0$：备用配置在平均响应时间上具有不劣于A的证据；
- $U_{0.95}>0$：不能宣称其平均响应时间不劣于A，应如实报告备用保障所付出的响应代价。

无论检验结果如何，策略C都作为“备用车辆配置”单独报告；日常优化方案则在无固定备用身份的A、B之间按平均响应时间选择。这样既回答题面要求，也不把备用保障与覆盖保护性派车混成同一方案。

实际筛选得到的备用向量与阈值为

$$
r^*=(0,0,1,0,0,0),\qquad \tau^*=7\ \text{min},
$$

即S3的两辆车中固定1辆承担备用角色。当预计最快常规车响应超过7 min，且该备用车能更早到场时才解除保护；任务结束回S3后恢复备用身份。最终评价中，策略C平均响应时间为5.5872 min，与A的成对差值为 $-0.0271$ min，双侧95%置信区间为 $[-0.0487,-0.0055]$ min；单侧95%上限为 $-0.0091$ min。因此在本仿真情景下，该备用配置没有牺牲平均响应时间，但其严格4分钟率略低于A，仍应作为备用保障方案而非总体最优派车方案。

### 4.8 连续多日、预热期与独立复制

#### 4.8.1 为什么不能模拟独立的单日

若每天00:00都把车辆设为空闲并清空队列，会系统性低估凌晨响应时间；若24:00停止尚未完成的任务，会丢失最拥堵时段的数据。因此采用连续多日仿真。

#### 4.8.2 固定30天预热

本题数据规模较小，任务二重点是派车与备用规则。为避免预热期估计喧宾夺主，统一固定

$$
W=30\ \text{天}.
$$

每个独立复制先连续运行30天，再从第31天到达的呼叫开始统计。预热期只删除指标，不重置车辆状态或FCFS队列；第30天遗留的忙车和未派呼叫继续影响第31天。所有候选策略使用同一预热长度，保证比较口径一致。

#### 4.8.3 参数筛选与最终评价

实验采用两层结构：

- 参数筛选：3个独立复制，每个复制为“30天预热＋7个统计日＋日末排空”，用于B参数和C配置选择；
- 最终评价：30个全新复制，每个复制为“30天预热＋30个统计日＋日末排空”，用于评价A、入选B和入选C；
- 同一复制内A、B、C使用完全相同的呼叫到达时刻和区域标记，即共同随机数；
- 不同复制使用不同种子，筛选与最终评价种子互不重叠。

C先在筛选集的47个非零备用向量与5个阈值中选出唯一候选；B先比较20组粗网格，再在优胜点附近细化。B、C参数一旦进入最终评价即冻结。

日常主方案在A、B之间按题面“优化后的平均响应时间”确定。先排除违反硬约束或存在持续发散积压的方案，再比较最终评价中的平均响应时间。若两者成对差值的双侧95%置信区间包含0，则视为均值证据不足以区分，依次比较严格4分钟服务率、P95响应时间和区域公平性。C作为备用配置单独评价其保障作用与响应代价。

### 4.9 评价指标与统计推断

题面给出的“距最近医院距离”不进入站点到呼叫点的响应距离，也不进入派车评分。对区域 $i$ 而言，该距离 $h_i$ 与派出哪个站点无关；在任务一目标中加入 $h_i$ 后，新增项为 $\sum_i h_i q_i$，由需求守恒可知是常数，不能改变最优站点服务分配。任务二又已给定45 min的完整车辆占用周期，若把到医院行程另行加到车辆忙碌时间会发生重复计时。因此仅将其用于补充评价，定义呼叫至最近医院的理想行驶链条时间

$$
T_e^{\mathrm{chain}}=T_e^{\mathrm{resp}}+\frac{60h_{i(e)}}{45}.
$$

该指标只叠加响应过程与按45 km/h直达最近医院的理想行驶时间，不含现场处置、交接和院内救治，不能解释为真实到院时间。主模型按题面说明使用区域中心坐标作为呼叫空间代理；这是聚合数据口径，不表示现实呼叫全部发生在中心点。

#### 4.9.1 主指标

$$
\bar T^{\mathrm{resp}}=\frac1N\sum_{e=1}^{N}T_e^{\mathrm{resp}}.
$$

任务二最终“优化后的平均响应时间”必须来自测试集逐呼叫仿真，不能用任务一的静态距离公式代替。

#### 4.9.2 延迟惩罚成本

题面给出的延迟惩罚标准为200元/(min·次)。以4 min黄金响应时限为起罚点，呼叫 $e$ 的惩罚成本定义为

$$
C_e=200\max\left(T_e^{\mathrm{resp}}-4,0\right)\quad(\text{元}).
$$

该指标只惩罚超过4 min的部分：4 min及以内成本为0，响应5.5 min对应300元。它是辅助评价指标，不替代“平均响应时间”主目标。最终同时报告平均每次惩罚成本

$$
\bar C=\frac1N\sum_{e=1}^{N}C_e
$$

和日均惩罚总成本

$$
\bar C_{\mathrm{day}}=\frac1D\sum_{e=1}^{N}C_e.
$$

#### 4.9.3 其他辅助指标

- 严格4分钟服务率 $N^{-1}\sum_e\mathbf1(T_e^{\mathrm{resp}}\le4)$；
- 响应时间中位数、P90、P95；
- 平均等待时间、发生等待的呼叫比例、最大等待时间；
- 日末平均积压量、最大队列长度、积压清空时间；
- 各区域平均响应时间及区域间最大差；
- 各车日接单次数分布、达到12次上限的比例；
- 每车占用率与站点负荷分布；
- 备用车启用次数、启用原因和启用后的实际响应改善。
- 平均每次延迟惩罚成本与日均延迟惩罚总成本。

#### 4.9.4 置信区间

以“独立复制”而不是把全部呼叫错误地当作独立样本作为统计单位。对每个指标报告复制均值的95%置信区间。策略差异使用共同随机数下的成对差值，例如

$$
\Delta_m^{B-A}=M_{B,m}-M_{A,m},
$$

再对 $\Delta_m$ 构造t置信区间。A与B的主要比较使用双侧95%区间；C相对A的非劣诊断使用单侧95%上限。最终评价只使用筛选阶段已经冻结的参数，不再重新调参。

#### 4.9.5 最终评价结果

每种方案基于30个独立复制；每个复制固定预热30天，随后统计30天，共包含4200次呼叫。表中区间均为以独立复制为统计单位的95%置信区间。

| 指标 | A：全局就近 | B：前瞻保护 | C：固定备用 |
|---|---:|---:|---:|
| 平均响应时间/min | 5.6142 [5.5577, 5.6708] | **5.3432 [5.3111, 5.3753]** | 5.5872 [5.5400, 5.6343] |
| 严格4分钟服务率/% | 43.13 | **44.92** | 42.94 |
| P90响应时间/min | 8.5130 | **8.4975** | **8.4975** |
| P95响应时间/min | 10.9949 | **9.9006** | 10.8021 |
| 平均等待时间/min | 0.4053 | **0.2225** | 0.3657 |
| 平均每次延迟惩罚/元 | 409.08 | **358.44** | 403.27 |
| 日均延迟惩罚总成本/元 | 57271.05 | **50181.83** | 56458.12 |
| 日末平均积压/次 | 0.7078 | **0.3700** | 0.6289 |
| 区域平均响应极差/min | 4.1578 | **3.9994** | 4.0822 |

策略B在平均响应、P95、等待、延迟成本、积压和区域公平性上均优于A，因此选为日常主方案。其平均每次延迟惩罚降低50.64元，日均总成本降低7089.22元，降幅均为12.38%。策略C完成了题面要求的明确备用车辆配置，并在不显著增加平均响应的前提下提供固定备用角色；但其严格4分钟率略低于A、总体指标弱于B，故单独作为备用保障方案报告。

修复结果表字段映射后，策略C的30个复制均记录到有效的备用派车次数；每个30天统计窗平均启用S3备用车358.67次，占4200次事故呼叫的8.54%。该数值只描述固定备用角色被解除保护的实际频率，不作为选择日常主方案的目标。

### 4.10 任务二程序事件循环

每个复制按以下流程实现：

1. 读取任务一固定站点、车辆和距离矩阵。
2. 用双高斯周期函数计算条件NHPP日内密度，按种子为每天生成固定140次呼叫。
3. 初始化12辆车为空闲、日接单数为0；随后只靠事件推进状态。
4. 事件日历包含呼叫到达、车辆任务完成、午夜计数切换和仿真到达窗口结束。
5. 每批事件后调用统一FCFS派车器；派车器内部只替换A、B或C的车辆选择函数。
6. 接受派车时检查车辆空闲、日接单数小于12、呼叫确为队首；随后计算等待和响应时间并把车辆锁定45 min。
7. 00:00只重置日计数；不清空队列、不释放忙车。
8. 测量窗结束后停止新到达，继续处理全部旧呼叫直至完成。
9. 输出逐呼叫、逐车辆、逐日和逐复制四级结果，运行硬约束检查。

### 4.11 任务二硬约束检查与失败条件

每次仿真必须满足：

- 生成的每个呼叫最终且仅被派车一次；
- 每个日历日恰好生成140次呼叫；
- 每辆车任一日接受任务数不超过12；
- 同一辆车的任务占用区间不重叠；
- 任何后到呼叫不得越过尚未派出的队首；
- 任务恰在接受派车45 min后恢复可用；
- 车辆恢复位置为所属站点；
- 呼叫日与派车计数日分别按到达和接受派车时刻归属；
- 调参、方案选择和最终测试随机种子两两无重叠；
- 策略比较使用同一复制的共同随机呼叫流；
- 统计窗后的排空呼叫仍计入其到达日指标。

以下实现直接判错：

- 24:00截断任务或清空队列；
- 每日从“全部车辆空闲”重新开始；
- 把45 min当现场时间后再加往返；
- 车辆完成任务后留在呼叫区域或任意跨站驻留；
- 方案B私自设置备用车，或方案C叠加B的覆盖评分；
- 把双高斯系数称为历史拟合结果；
- 用最终评价集重新调参，或查看最终结果后更改参数网格；
- 只报告单次均值而无P95、覆盖率和置信区间；
- 固定推荐两辆备用车而未比较全部合法配置。

## 5. 两问统一实现合同

### 5.1 输入

- 原题10个区域的坐标和 $q_i$；
- 6个站点坐标、最大配车数；
- 速度45 km/h、准备3 min、占用45 min、每车每日12次、黄金时限4 min；
- 本报告给出的双高斯合成参数、筛选与最终评价两组随机种子和策略参数网格。

### 5.2 任务一输出

- 车辆配置与开站向量；
- $10\times6$ 距离矩阵和服务分配矩阵；
- 站点负荷、容量余量；
- $D^*$、$\bar d$、静态响应下界；
- 三种覆盖率的分口径结果；
- 约束残差和求解器状态。

### 5.3 任务二输出

- 完整24维条件NHPP小时概率和峰值敏感性情景；
- 固定30天预热设定及跨日状态继承检查；
- B的20组粗网格及细化参数筛选表、C的235种配置筛选表；
- A、调参后B和最佳C的最终评价表及日常主方案入选证据；
- 最终评价均值、覆盖率、P90/P95、等待、公平性和负荷；
- 成对差值及95%置信区间；
- C相对A的单侧非劣诊断与备用保障代价；
- 平均每次与日均延迟惩罚成本；
- 逐呼叫结果和全部硬约束检查日志。

### 5.4 当前已确定与尚待计算

已经确定：

- 第一问模型、必要性证明、两阶段目标和完整复算结果；
- 第二问每日固定140次的条件NHPP、双高斯日内强度、连续多日边界、45 min占用与延迟惩罚口径；
- A/B/C三种策略的数学规则；
- 固定30天预热、筛选/最终评价分离、共同随机数和置信区间设计。

尚待编程阶段真实运行后确定：

- 任务三应急切换规则及事故情景下的动态结果。

任务二已确定：B取 $(\beta,\delta)=(4,2)$ 并成为日常主方案；C取 $r=(0,0,1,0,0,0)$、$\tau=7$ min，作为独立备用保障方案。

## 6. 文献依据

1. Matteson, D. S., McLean, M. W., Woodard, D. B., & Henderson, S. G. (2011). Forecasting emergency medical service call arrival rates. *The Annals of Applied Statistics*, 5(2B). DOI: `10.1214/10-AOAS442`。用于说明急救呼叫应按计数型、时变到达率建模；本题因缺少逐时数据，只采用透明合成NHPP情景，不声称复现其估计方法。
2. Jánošíková, Ľ., Jankovič, P., Kvet, M., & Zajacová, F. (2021). Coverage versus response time objectives in ambulance location. *International Journal of Health Geographics*, 20, 32. DOI: `10.1186/s12942-021-00285-x`。支持将平均响应/距离与时限覆盖分开评价，并用仿真检验静态选址解。
3. Wu, C.-H., & Hwang, K. P. (2009). Using a discrete-event simulation to balance ambulance availability and demand in static deployment systems. *Academic Emergency Medicine*, 16(12), 1359--1366. DOI: `10.1111/j.1553-2712.2009.00583.x`。支持在静态常驻配置下以离散事件仿真评估车辆可用性、需求与响应时间。
4. Yang, W., Su, Q., Huang, S. H., Wang, Q., Zhu, Y., & Zhou, M. (2019). Simulation modeling and optimization for ambulance allocation considering spatiotemporal stochastic demand. *Journal of Management Science and Engineering*, 4(4), 252--265. DOI: `10.1016/j.jmse.2020.01.004`。支持用仿真优化处理时空随机需求；本题不继承其数据或数值结论。

以上元数据已由OpenAlex与AnySearch交叉匹配，并通过Crossref DOI接口核对题名、作者、年份和期刊。

## 7. 任务三的条件输入边界

任务三题面要求“对现有调度方案进行动态调整”，因此不在任务二结果产生前预先指定第三问一定从A或B出发。第三问将以任务二最终评价选出的日常主方案为基线，并把策略C得到的备用向量与阈值作为可调用的备用保障信息，而不把两种派车评分机械叠加。

已经冻结且不依赖任务二结果的第三问边界只有：事故区域遍历全部10个区域；事故期间该区域NHPP强度变为正常值5倍；持续时间作为变量绘制事故期性能曲线；只使用现有12辆车；允许跨站派车但任务后回所属站；不凭空添加事故时段、外援车辆、伤情比例或任务耗时增幅。具体应急切换规则必须等任务二真实结果后再定型。

## 8. 作者自检记录

- 原题和全部现有附件已核对，输入文件保持只读，原题SHA-256见报告首页；
- 任务一使用一个容量约束MILP/运输LP模型族，任务二使用NHPP驱动离散事件仿真并比较三种派车策略，未超过每题两个独立模型体系；
- 变量、单位、目标、约束、跨日状态、调参与最终评价边界均已列明；
- 第一问运输LP已有真实求解结果，第二问的动态数值明确标为待代码运行，不预写结论；
- 失败条件涵盖需求守恒、车辆容量、日接单上限、FCFS、45 min占用、跨日继承、随机种子隔离和统计推断；
- 文献条目保留DOI与适用范围，正式M1仍须独立核验可追溯性；
- 固定产物仅为本报告与《术语表格》，均位于项目目录，未改写skill目录。
