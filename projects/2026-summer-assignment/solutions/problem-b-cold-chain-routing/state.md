# Current State

## Objective

从 B 题原题建立完整、可复现的冷链配送解决方案，并形成模型、代码、证据图表和论文。

## Phase

数值结果已验证并保持冻结；论文 Markdown 和 Word 已完成本轮修订，但仍是等待人工内容审阅的论文草稿。当前软件环境下的结构、格式和逐页渲染检查已通过，不等同于已验证所有 Word/WPS 环境中的版式一致性。

## Accepted Semantics

- `decisions.md` 中 DEC-B-001 至 DEC-B-003 继续有效：固定三车分区和配送站必经结构；按迟到门店数、再按综合成本作词典序优化；任务三按原题有向弧 `2→8` 严格封闭。
- 本轮未修改、替代或新增实质语义决策。

## Verified Numerical Results

- 标准库求解器精确枚举全部 8 个完整路线组合。正常最优路线为 A `0→1→4→6→0`、B `0→2→5→8→0`、C `0→3→9→7→0`。
- 正常方案总里程为 `103.8713 km`，综合成本为 `415.4852 元`，早到和迟到事件均为 0。
- 封闭 `2→8` 后，原最优路线仍可行，因此最优路线、里程、成本和时间窗指标增量均为 0。
- 统一门店服务时长保持零迟到的上限约为 `21.42054 min/店`。
- 7 项求解器回归测试于本轮全部通过，覆盖全枚举最优性、手算距离、到达时刻、禁弧剔除、零成本差和服务时长阈值。

## Paper Draft Status

- `paper/paper.md` 已把候选路线枚举改写为闭合的路线型 0-1 模型：候选路线先递推路线级到达/服务时刻并生成早迟到、里程、载重和禁弧系数，再由唯一选路变量聚合为实际方案并施加可行性与词典序目标。
- 图 2 注释已移入图框并重新生成；论文中的数值结果未改变。
- `src/build_paper.py` 是当前 Markdown 到 Word 的单一复现入口，依次调用 Pandoc 和仓库后处理器；不再生成或依赖包含本机绝对路径的转换清单，原 `paper/paper.conversion.json` 已删除。
- `paper/paper.docx` 已由当前 `paper.md` 重新生成。论文仍为草稿，尚待人工确认论证表达和提交口径。

## Environment-Dependent Layout Checks

- 本轮使用仓库 Python 3.13、`python-docx 1.2.0` 和 PATH 中的 Pandoc 3.9 构建 DOCX；结构校验通过。
- 在当前 LibreOffice 渲染环境下得到 14 页、3 图、4 表和 116 个公式；通用数学建模格式检查通过，14 页均已逐页目视检查，未发现图注裁切、表格断裂、溢出或异常分页。
- 上述结论只证明当前 Pandoc、字体和 LibreOffice 渲染组合下的版式。Microsoft Word 或 WPS 的字体替换与分页可能不同，正式提交前仍需在目标软件中打开并检查一次。

## Next Actions

1. 人工复核论文论证表达以及题意解释是否符合预期。
2. 若作为正式稿提交，在目标 Word/WPS 环境检查分页，并按实际提交规则核对模板和匿名要求。

## Blockers

- 无。

## Pending Decisions

- 无。

## Operational Boundaries

- 不为制造非零绕路代价而改变原题封闭弧或强制路线。
- 不为 8 个候选组合引入元启发式、大规模随机实验或无消费者的复现产物。
